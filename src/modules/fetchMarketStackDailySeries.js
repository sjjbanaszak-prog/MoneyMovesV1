// fetchMarketStackDailySeries.js
// MarketStack integration has been removed. This stub preserves the export
// so existing call sites (PortfolioBuilder) continue to compile without errors.

export const fetchMarketStackDailySeries = async (_ticker) => {
  return {};
};
